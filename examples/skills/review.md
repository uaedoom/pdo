description: Review the code in the current directory for bugs and improvements

You are doing a focused code review. Use your tools to inspect the files in the
current working directory, then report the most important findings as a short
Markdown list, each with a `file:line` reference. Prioritise correctness bugs,
then security issues, then simplifications. Be concise.

{{args}}
